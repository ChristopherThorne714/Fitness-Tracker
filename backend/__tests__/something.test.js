// jest.mock('../middleware/AuthMiddleware', () => ({
//     userVerification: () => jest.fn((req, res, next) => next()),
//     reqAuth: () => jest.fn((req, res, next) => next())
// }));

describe("something going on in here", () => {
    test("something something sjhasdlk;fj", async () => {
        expect(1+1).toBe(2);
    });
});